<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student.mybook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Category" />
        <activity android:name=".Money_planner1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Money_planner2" />
        <activity android:name=".plans" />
        <activity android:name=".book" />
        <activity android:name=".Add_Plans" />
        <activity android:name=".BooksMain" />
        <activity android:name=".AddNewBook" />
        <activity android:name=".Book" />
        <activity android:name=".UpdateBook"></activity>
    </application>

</manifest>